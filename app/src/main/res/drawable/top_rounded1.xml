<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- lower rectangle (border color) -->
    <item>
        <shape >
            <solid android:color="@color/colorBcg" />
        </shape>
    </item>

    <!-- upper rectangle (background color) -->
    <item>
        <shape >
            <corners android:topLeftRadius="@dimen/corner_radius_size"
            android:topRightRadius="@dimen/corner_radius_size" ></corners>
            <solid android:color="@color/colorMonday"/>
        </shape>
    </item>
</layer-list>